#!/bin/csh
set LIST = (algebrbf beetlebf complbf epbf epinf fpbf fpinf integrbf ifaces lcbf lcinf linbf linfpbf psbf psinf)
set HOMED=`pwd`
foreach DIR ($LIST)
  echo "Directory $HOMED/$DIR"
  cd $HOMED/$DIR;$HOMED/scripts/mklist
end
