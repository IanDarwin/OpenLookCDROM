/*
 * export_ui.c - User interface object initialization functions.
 * This file was generated by `gxv' from `export.G'.
 * DO NOT EDIT BY HAND.
 */

#include <stdio.h>
#include <sys/param.h>
#include <sys/types.h>
#include <xview/xview.h>
#include <xview/canvas.h>
#include <xview/panel.h>
#include <xview/scrollbar.h>
#include <xview/svrimage.h>
#include <xview/termsw.h>
#include <xview/text.h>
#include <xview/tty.h>
#include <xview/xv_xrect.h>
#include "export_ui.h"

/*
 * Initialize an instance of object `exportPopup'.
 */
export_exportPopup_objects *
export_exportPopup_objects_initialize(export_exportPopup_objects *ip, Xv_opaque owner)
{
	if (!ip && !(ip = (export_exportPopup_objects *) calloc(1, sizeof (export_exportPopup_objects))))
		return (export_exportPopup_objects *) NULL;
	if (!ip->exportPopup)
		ip->exportPopup = export_exportPopup_exportPopup_create(ip, owner);
	if (!ip->control)
		ip->control = export_exportPopup_control_create(ip, ip->exportPopup);
	if (!ip->directory)
		ip->directory = export_exportPopup_directory_create(ip, ip->control);
	if (!ip->file)
		ip->file = export_exportPopup_file_create(ip, ip->control);
	if (!ip->applyButton)
		ip->applyButton = export_exportPopup_applyButton_create(ip, ip->control);
	return ip;
}

/*
 * Create object `exportPopup' in the specified instance.
 */
Xv_opaque
export_exportPopup_exportPopup_create(export_exportPopup_objects *ip, Xv_opaque owner)
{
	extern void	export_exportPopup_done_callback(Frame);
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME_CMD,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 343,
		XV_HEIGHT, 105,
		XV_LABEL, "Export Options",
		XV_SHOW, FALSE,
		FRAME_SHOW_FOOTER, TRUE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		FRAME_CMD_PUSHPIN_IN, TRUE,
		FRAME_DONE_PROC, export_exportPopup_done_callback,
		NULL);
	xv_set(xv_get(obj, FRAME_CMD_PANEL), WIN_SHOW, FALSE, NULL);
	return obj;
}

/*
 * Create object `control' in the specified instance.
 */
Xv_opaque
export_exportPopup_control_create(export_exportPopup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "export:control",
		XV_X, 0,
		XV_Y, 0,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		WIN_BORDER, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `directory' in the specified instance.
 */
Xv_opaque
export_exportPopup_directory_create(export_exportPopup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "export:directory",
		XV_X, 16,
		XV_Y, 16,
		PANEL_VALUE_DISPLAY_LENGTH, 30,
		PANEL_VALUE_STORED_LENGTH, 100,
		PANEL_LABEL_STRING, "Directory:",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `file' in the specified instance.
 */
Xv_opaque
export_exportPopup_file_create(export_exportPopup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "export:file",
		XV_X, 16,
		XV_Y, 48,
		PANEL_VALUE_DISPLAY_LENGTH, 30,
		PANEL_VALUE_STORED_LENGTH, 100,
		PANEL_LABEL_STRING, "File:",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `applyButton' in the specified instance.
 */
Xv_opaque
export_exportPopup_applyButton_create(export_exportPopup_objects *ip, Xv_opaque owner)
{
	extern void		export_apply_callback(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "export:applyButton",
		XV_X, 144,
		XV_Y, 80,
		PANEL_LABEL_STRING, "Apply",
		PANEL_NOTIFY_PROC, export_apply_callback,
		NULL);
	return obj;
}

