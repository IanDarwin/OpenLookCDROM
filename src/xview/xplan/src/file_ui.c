/*
 * file_ui.c - User interface object initialization functions.
 * This file was generated by `gxv' from `file.G'.
 * DO NOT EDIT BY HAND.
 */

#include <stdio.h>
#include <sys/param.h>
#include <sys/types.h>
#include <xview/xview.h>
#include <xview/canvas.h>
#include <xview/panel.h>
#include <xview/scrollbar.h>
#include <xview/svrimage.h>
#include <xview/termsw.h>
#include <xview/text.h>
#include <xview/tty.h>
#include <xview/xv_xrect.h>
#include "file_ui.h"

/*
 * Initialize an instance of object `filePopup'.
 */
file_filePopup_objects *
file_filePopup_objects_initialize(file_filePopup_objects *ip, Xv_opaque owner)
{
	if (!ip && !(ip = (file_filePopup_objects *) calloc(1, sizeof (file_filePopup_objects))))
		return (file_filePopup_objects *) NULL;
	if (!ip->filePopup)
		ip->filePopup = file_filePopup_filePopup_create(ip, owner);
	if (!ip->control)
		ip->control = file_filePopup_control_create(ip, ip->filePopup);
	if (!ip->directoryField)
		ip->directoryField = file_filePopup_directoryField_create(ip, ip->control);
	if (!ip->fileList)
		ip->fileList = file_filePopup_fileList_create(ip, ip->control);
	if (!ip->fileField)
		ip->fileField = file_filePopup_fileField_create(ip, ip->control);
	if (!ip->applyButton)
		ip->applyButton = file_filePopup_applyButton_create(ip, ip->control);
	return ip;
}

/*
 * Create object `filePopup' in the specified instance.
 */
Xv_opaque
file_filePopup_filePopup_create(file_filePopup_objects *ip, Xv_opaque owner)
{
	extern void	filePopup_done_callback(Frame);
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME_CMD,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 343,
		XV_HEIGHT, 318,
		XV_LABEL, "Default",
		XV_SHOW, FALSE,
		FRAME_SHOW_FOOTER, TRUE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		FRAME_CMD_PUSHPIN_IN, TRUE,
		FRAME_DONE_PROC, filePopup_done_callback,
		NULL);
	xv_set(xv_get(obj, FRAME_CMD_PANEL), WIN_SHOW, FALSE, NULL);
	return obj;
}

/*
 * Create object `control' in the specified instance.
 */
Xv_opaque
file_filePopup_control_create(file_filePopup_objects *ip, Xv_opaque owner)
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "file:control",
		XV_X, 0,
		XV_Y, 0,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		WIN_BORDER, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `directoryField' in the specified instance.
 */
Xv_opaque
file_filePopup_directoryField_create(file_filePopup_objects *ip, Xv_opaque owner)
{
	extern Panel_setting	directory_prompt_notify(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "file:directoryField",
		XV_X, 24,
		XV_Y, 16,
		PANEL_VALUE_DISPLAY_LENGTH, 30,
		PANEL_VALUE_STORED_LENGTH, 256,
		PANEL_LABEL_STRING, "Directory:",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, FALSE,
		PANEL_NOTIFY_PROC, directory_prompt_notify,
		NULL);
	return obj;
}

/*
 * Create object `fileList' in the specified instance.
 */
Xv_opaque
file_filePopup_fileList_create(file_filePopup_objects *ip, Xv_opaque owner)
{
	extern int		fileList_notify_callback(Panel_item, char *, Xv_opaque, Panel_list_op, Event *, int);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_LIST,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "file:fileList",
		XV_X, 24,
		XV_Y, 40,
		PANEL_LIST_WIDTH, 300,
		PANEL_LIST_DISPLAY_ROWS, 10,
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, TRUE,
		PANEL_CHOOSE_ONE, TRUE,
		PANEL_CHOOSE_NONE, TRUE,
		PANEL_NOTIFY_PROC, fileList_notify_callback,
		NULL);
	return obj;
}

/*
 * Create object `fileField' in the specified instance.
 */
Xv_opaque
file_filePopup_fileField_create(file_filePopup_objects *ip, Xv_opaque owner)
{
	extern Panel_setting	file_prompt_notify(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "file:fileField",
		XV_X, 24,
		XV_Y, 256,
		PANEL_VALUE_DISPLAY_LENGTH, 30,
		PANEL_VALUE_STORED_LENGTH, 30,
		PANEL_LABEL_STRING, "File:",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, FALSE,
		PANEL_NOTIFY_PROC, file_prompt_notify,
		NULL);
	return obj;
}

/*
 * Create object `applyButton' in the specified instance.
 */
Xv_opaque
file_filePopup_applyButton_create(file_filePopup_objects *ip, Xv_opaque owner)
{
	extern void		applyButton_callback(Panel_item, Event *);
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "file:applyButton",
		XV_X, 144,
		XV_Y, 288,
		PANEL_LABEL_STRING, "Default",
		PANEL_NOTIFY_PROC, applyButton_callback,
		NULL);
	return obj;
}

