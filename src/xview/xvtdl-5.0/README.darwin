We modified gcm.c, xdefaults.c, and added a default yywrap() for
systems that lack it (in the grammar file).

The source code is more daunting than necessary since there is
another copy of the devGuide library here; this will be cleaned
up in our next release.

