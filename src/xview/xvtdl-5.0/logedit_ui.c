/*
 * logedit_ui.c - User interface object initialization functions.
 * This file was generated by `gxv' from `logedit.G'.
 * DO NOT EDIT BY HAND.
 */

#include <stdio.h>
#include <sys/param.h>
#include <sys/types.h>
#include <xview/xview.h>
#include <xview/canvas.h>
#include <xview/panel.h>
#include <xview/scrollbar.h>
#include <xview/svrimage.h>
#include <xview/termsw.h>
#include <xview/text.h>
#include <xview/tty.h>
#include <xview/xv_xrect.h>
#include "logedit_ui.h"

/*
 * Initialize an instance of object `log_edit'.
 */
logedit_log_edit_objects *
logedit_log_edit_objects_initialize(ip, owner)
	logedit_log_edit_objects	*ip;
	Xv_opaque	owner;
{
	if (!ip && !(ip = (logedit_log_edit_objects *) calloc(1, sizeof (logedit_log_edit_objects))))
		return (logedit_log_edit_objects *) NULL;
	if (!ip->log_edit)
		ip->log_edit = logedit_log_edit_log_edit_create(ip, owner);
	if (!ip->log_editor)
		ip->log_editor = logedit_log_edit_log_editor_create(ip, ip->log_edit);
	if (!ip->controls1)
		ip->controls1 = logedit_log_edit_controls1_create(ip, ip->log_edit);
	if (!ip->log_editor_cancel)
		ip->log_editor_cancel = logedit_log_edit_log_editor_cancel_create(ip, ip->controls1);
	if (!ip->log_editor_done)
		ip->log_editor_done = logedit_log_edit_log_editor_done_create(ip, ip->controls1);
	return ip;
}

/*
 * Create object `log_edit' in the specified instance.
 */
Xv_opaque
logedit_log_edit_log_edit_create(ip, owner)
	logedit_log_edit_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME_CMD,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 532,
		XV_HEIGHT, 334,
		XV_LABEL, "Edit Log Entry",
		FRAME_SHOW_FOOTER, TRUE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		FRAME_CMD_PUSHPIN_IN, FALSE,
		NULL);
	xv_set(xv_get(obj, FRAME_CMD_PANEL), WIN_SHOW, FALSE, NULL);
	return obj;
}

/*
 * Create object `log_editor' in the specified instance.
 */
Xv_opaque
logedit_log_edit_log_editor_create(ip, owner)
	logedit_log_edit_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, TEXTSW,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "logedit:log_editor",
		XV_X, 0,
		XV_Y, 0,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, 291,
		OPENWIN_SHOW_BORDERS, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `controls1' in the specified instance.
 */
Xv_opaque
logedit_log_edit_controls1_create(ip, owner)
	logedit_log_edit_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "logedit:controls1",
		XV_X, 0,
		XV_Y, (int)xv_get(ip->log_editor, XV_Y) +
		      (int)xv_get(ip->log_editor, XV_HEIGHT),
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		WIN_BORDER, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `log_editor_cancel' in the specified instance.
 */
Xv_opaque
logedit_log_edit_log_editor_cancel_create(ip, owner)
	logedit_log_edit_objects	*ip;
	Xv_opaque	owner;
{
	extern void		log_log_edit();
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "logedit:log_editor_cancel",
		XV_X, 123,
		XV_Y, 13,
		PANEL_LABEL_STRING, "Cancel",
		PANEL_NOTIFY_PROC, log_log_edit,
		NULL);
	return obj;
}

/*
 * Create object `log_editor_done' in the specified instance.
 */
Xv_opaque
logedit_log_edit_log_editor_done_create(ip, owner)
	logedit_log_edit_objects	*ip;
	Xv_opaque	owner;
{
	extern void		log_log_edit();
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "logedit:log_editor_done",
		XV_X, 363,
		XV_Y, 13,
		PANEL_LABEL_STRING, "Done",
		PANEL_NOTIFY_PROC, log_log_edit,
		NULL);
	return obj;
}

