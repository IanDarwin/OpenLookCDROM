/*
 * deadline_ui.c - User interface object initialization functions.
 * This file was generated by `gxv' from `deadline.G'.
 * DO NOT EDIT BY HAND.
 */

#include <stdio.h>
#include <sys/param.h>
#include <sys/types.h>
#include <xview/xview.h>
#include <xview/canvas.h>
#include <xview/panel.h>
#include <xview/scrollbar.h>
#include <xview/svrimage.h>
#include <xview/termsw.h>
#include <xview/text.h>
#include <xview/tty.h>
#include <xview/xv_xrect.h>
#include "deadline_ui.h"

/*
 * Initialize an instance of object `deadline_frame'.
 */
deadline_deadline_frame_objects *
deadline_deadline_frame_objects_initialize(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	if (!ip && !(ip = (deadline_deadline_frame_objects *) calloc(1, sizeof (deadline_deadline_frame_objects))))
		return (deadline_deadline_frame_objects *) NULL;
	if (!ip->deadline_frame)
		ip->deadline_frame = deadline_deadline_frame_deadline_frame_create(ip, owner);
	if (!ip->deadline_panel)
		ip->deadline_panel = deadline_deadline_frame_deadline_panel_create(ip, ip->deadline_frame);
	if (!ip->deadline_date)
		ip->deadline_date = deadline_deadline_frame_deadline_date_create(ip, ip->deadline_panel);
	if (!ip->deadline_actions)
		ip->deadline_actions = deadline_deadline_frame_deadline_actions_create(ip, ip->deadline_panel);
	if (!ip->deadline_delete_units)
		ip->deadline_delete_units = deadline_deadline_frame_deadline_delete_units_create(ip, ip->deadline_panel);
	if (!ip->deadline_delete_time)
		ip->deadline_delete_time = deadline_deadline_frame_deadline_delete_time_create(ip, ip->deadline_panel);
	if (!ip->deadline_up_increment)
		ip->deadline_up_increment = deadline_deadline_frame_deadline_up_increment_create(ip, ip->deadline_panel);
	if (!ip->deadline_down_increment)
		ip->deadline_down_increment = deadline_deadline_frame_deadline_down_increment_create(ip, ip->deadline_panel);
	if (!ip->deadline_on_mail_to)
		ip->deadline_on_mail_to = deadline_deadline_frame_deadline_on_mail_to_create(ip, ip->deadline_panel);
	if (!ip->deadline_after_mail_to)
		ip->deadline_after_mail_to = deadline_deadline_frame_deadline_after_mail_to_create(ip, ip->deadline_panel);
	if (!ip->deadline_move_units)
		ip->deadline_move_units = deadline_deadline_frame_deadline_move_units_create(ip, ip->deadline_panel);
	if (!ip->deadline_move_after)
		ip->deadline_move_after = deadline_deadline_frame_deadline_move_after_create(ip, ip->deadline_panel);
	if (!ip->deadline_cancel)
		ip->deadline_cancel = deadline_deadline_frame_deadline_cancel_create(ip, ip->deadline_panel);
	if (!ip->deadline_done)
		ip->deadline_done = deadline_deadline_frame_deadline_done_create(ip, ip->deadline_panel);
	return ip;
}

/*
 * Create object `deadline_frame' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_frame_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, FRAME_CMD,
		XV_KEY_DATA, INSTANCE, ip,
		XV_WIDTH, 411,
		XV_HEIGHT, 265,
		XV_LABEL, "Deadline Editor",
		FRAME_SHOW_FOOTER, TRUE,
		FRAME_SHOW_RESIZE_CORNER, TRUE,
		FRAME_CMD_PUSHPIN_IN, FALSE,
		NULL);
	xv_set(xv_get(obj, FRAME_CMD_PANEL), WIN_SHOW, FALSE, NULL);
	return obj;
}

/*
 * Create object `deadline_panel' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_panel_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_panel",
		XV_X, 0,
		XV_Y, 0,
		XV_WIDTH, WIN_EXTEND_TO_EDGE,
		XV_HEIGHT, WIN_EXTEND_TO_EDGE,
		WIN_BORDER, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `deadline_date' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_date_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_date",
		XV_X, 22,
		XV_Y, 18,
		PANEL_VALUE_DISPLAY_LENGTH, 10,
		PANEL_VALUE_STORED_LENGTH, 15,
		PANEL_LABEL_STRING, "Deadline:",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_READ_ONLY, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `deadline_actions' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_actions_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	extern void		change_deadline_actions();
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TOGGLE,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_actions",
		XV_X, 22,
		XV_Y, 41,
		PANEL_CHOICE_NCOLS, 1,
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_LABEL_STRING, "Action:",
		PANEL_NOTIFY_PROC, change_deadline_actions,
		PANEL_CHOICE_STRINGS,
			"Delete",
			"+ Priority",
			"- Priority",
			"Mail on",
			"Mail After",
			"Move After",
			NULL,
		PANEL_VALUE, 0,
		NULL);
	return obj;
}

/*
 * Create object `deadline_delete_units' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_delete_units_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_CHOICE, PANEL_DISPLAY_LEVEL, PANEL_CURRENT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_delete_units",
		XV_X, 280,
		XV_Y, 42,
		PANEL_CHOICE_NROWS, 1,
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_CHOICE_STRINGS,
			"days",
			"weeks",
			"months",
			"years",
			NULL,
		PANEL_INACTIVE, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `deadline_delete_time' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_delete_time_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_NUMERIC_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_delete_time",
		XV_X, 165,
		XV_Y, 46,
		PANEL_VALUE_DISPLAY_LENGTH, 2,
		PANEL_VALUE_STORED_LENGTH, 4,
		PANEL_LABEL_STRING, "after",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_MAX_VALUE, 100,
		PANEL_MIN_VALUE, 0,
		PANEL_VALUE, 0,
		PANEL_INACTIVE, TRUE,
		PANEL_READ_ONLY, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `deadline_up_increment' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_up_increment_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_NUMERIC_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_up_increment",
		XV_X, 165,
		XV_Y, 75,
		PANEL_VALUE_DISPLAY_LENGTH, 1,
		PANEL_VALUE_STORED_LENGTH, 1,
		PANEL_LABEL_STRING, "increment:",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_MAX_VALUE, 8,
		PANEL_MIN_VALUE, 0,
		PANEL_VALUE, 0,
		PANEL_INACTIVE, TRUE,
		PANEL_READ_ONLY, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `deadline_down_increment' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_down_increment_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_NUMERIC_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_down_increment",
		XV_X, 165,
		XV_Y, 104,
		PANEL_VALUE_DISPLAY_LENGTH, 1,
		PANEL_VALUE_STORED_LENGTH, 1,
		PANEL_LABEL_STRING, "increment:",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_MAX_VALUE, 8,
		PANEL_MIN_VALUE, 0,
		PANEL_VALUE, 0,
		PANEL_INACTIVE, TRUE,
		PANEL_READ_ONLY, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `deadline_on_mail_to' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_on_mail_to_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_on_mail_to",
		XV_X, 165,
		XV_Y, 132,
		PANEL_VALUE_DISPLAY_LENGTH, 20,
		PANEL_VALUE_STORED_LENGTH, 80,
		PANEL_LABEL_STRING, "Mail to:",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_INACTIVE, TRUE,
		PANEL_READ_ONLY, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `deadline_after_mail_to' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_after_mail_to_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_after_mail_to",
		XV_X, 165,
		XV_Y, 160,
		PANEL_VALUE_DISPLAY_LENGTH, 20,
		PANEL_VALUE_STORED_LENGTH, 80,
		PANEL_LABEL_STRING, "Mail to:",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_INACTIVE, TRUE,
		PANEL_READ_ONLY, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `deadline_move_units' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_move_units_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_CHOICE, PANEL_DISPLAY_LEVEL, PANEL_CURRENT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_move_units",
		XV_X, 266,
		XV_Y, 185,
		PANEL_CHOICE_NROWS, 1,
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_CHOICE_STRINGS,
			"days",
			"weeks",
			"months",
			"years",
			NULL,
		PANEL_INACTIVE, TRUE,
		NULL);
	return obj;
}

/*
 * Create object `deadline_move_after' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_move_after_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_NUMERIC_TEXT,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_move_after",
		XV_X, 165,
		XV_Y, 190,
		PANEL_VALUE_DISPLAY_LENGTH, 2,
		PANEL_VALUE_STORED_LENGTH, 4,
		PANEL_LABEL_STRING, "by",
		PANEL_LAYOUT, PANEL_HORIZONTAL,
		PANEL_MAX_VALUE, 100,
		PANEL_MIN_VALUE, 0,
		PANEL_VALUE, 0,
		PANEL_INACTIVE, TRUE,
		PANEL_READ_ONLY, FALSE,
		NULL);
	return obj;
}

/*
 * Create object `deadline_cancel' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_cancel_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	extern void		deadline_cancel_proc();
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_cancel",
		XV_X, 238,
		XV_Y, 226,
		PANEL_LABEL_STRING, "Cancel",
		PANEL_NOTIFY_PROC, deadline_cancel_proc,
		NULL);
	return obj;
}

/*
 * Create object `deadline_done' in the specified instance.
 */
Xv_opaque
deadline_deadline_frame_deadline_done_create(ip, owner)
	deadline_deadline_frame_objects	*ip;
	Xv_opaque	owner;
{
	extern void		deadline_done_proc();
	Xv_opaque	obj;
	
	obj = xv_create(owner, PANEL_BUTTON,
		XV_KEY_DATA, INSTANCE, ip,
		XV_HELP_DATA, "deadline:deadline_done",
		XV_X, 311,
		XV_Y, 226,
		PANEL_LABEL_STRING, "Done",
		PANEL_NOTIFY_PROC, deadline_done_proc,
		NULL);
	return obj;
}

