The file faxtool.stubs.c is the "real" stubs file. If you ever run
gxv, be sure to use the -a (ansi) switch AND THEN be sure to
	cp faxtool.stubs.c faxtool_stubs.c
before trying to compile faxtool_stubs.c.
