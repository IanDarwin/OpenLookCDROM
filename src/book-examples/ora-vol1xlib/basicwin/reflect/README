This example takes the basicwin window and creates a second window
that is a mirror image of it.  This is done using images, very slowly.
Expect to wait a few minutes for the mirron window to be drawn.
