PROGRAM = abob

AllTarget($(PROGRAM))
InstallProgram($(PROGRAM),$(BINDIR))
InstallManPage($(PROGRAM),$(MANDIR))
InstallNonExec(bob.help, $(BINDIR))
InstallNonExec(btaylor.bm, $(BINDIR))
DependTarget()
LintTarget()

clean::
