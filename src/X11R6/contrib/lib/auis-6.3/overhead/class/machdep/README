Notes:

Globals crap is essentially a table mapping symbol names (strings) to pointers to that symbol. Pointers are actually typeless 32 bit things...

Current interface is:

doload

dofix crap???

globals array?

globals.o and libcx.a?
