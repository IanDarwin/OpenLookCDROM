%include pobbconfig.defs
#
# ?[POBBRunWarning]
# ?[POBBRunNotice]
%ifdef pobb_NNTPIn

dir ?[NetDatabaseRoot]
groups ?[arrlist(NNTPGroups, ",")]

delayedgroups ?[arrlist(NNTPDelayedGroups, ",")]
peakhours ?[NNTPPeakHours]

bbroot ?[arrlist(PostedNetnewsRoots, "\nbbroot ")]
%endif
