class typescript[tscript]: textview[textv] {
  classprocedures:
    InitializeClass() returns boolean;
    InitializeObject(struct typescript *self) returns boolean;
    FinalizeObject(struct typescript *self);
};
